<form method="post" action="{{ path('test_entity_delete', {'id': test_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ test_entity.id) }}">
    <button class="btn">Delete</button>
</form>
